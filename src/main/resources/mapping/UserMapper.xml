<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">    
    
<mapper namespace="ac.cn.saya.laboratory.dao.UserDAO">

    <!--查询用户-->
    <select id="queryUser"  parameterType="String"  resultType="user">
        select * from user a where a.user = #{user} or a.email = #{user}
    </select>

    <!-- 修改用户资料  -->
    <update id="updateUser" parameterType="user">
        update user
        <set>
            <if test="password != null and password != ''">
                password = #{password},
            </if>
            <if test="sex != null and sex != ''">
                sex = #{sex},
            </if>
            <if test="qq != null and qq != ''">
                qq = #{qq},
            </if>
            <if test="email != null and email != ''">
                email = #{email},
            </if>
            <if test="phone != null and phone != ''">
                phone = #{phone},
            </if>
            <if test="birthday != null and birthday != ''">
                birthday = #{birthday},
            </if>
            <if test="hometown != null and hometown != ''">
                hometown = #{hometown},
            </if>
            <if test="autograph != null and autograph != ''">
                autograph = #{autograph},
            </if>
            <if test="logo != null and logo != ''">
                logo = #{logo},
            </if>
                updateTime = now()
        </set>
        where `user` = #{user}
    </update>

</mapper> 